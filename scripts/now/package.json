{
  "scripts": {
    "build": "echo \"dummy build\"",
    "build:app": "cd ../.. && npm run build",
    "deploy": "node build && npx now"
  },
  "dependencies": {
    "@now/node": "^1.7.1",
    "@sveltech/ssr": "^0.0.9",
    "fs-extra": "^8.1.0",
    "gray-matter": "^4.0.2",
    "markdown-it": "^11.0.0"
  }
}
